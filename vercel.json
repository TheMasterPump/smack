{
  "installCommand": "npm install --legacy-peer-deps",
  "buildCommand": "npx react-app-rewired build",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
